[package]
name = "storage-integration-tests"
version = { workspace = true }
edition = { workspace = true }
description = "Container-backed storage integration tests for the smista.ai workspace."
license = "Elastic-2.0"
publish = false

[dev-dependencies]
secrecy = { workspace = true }
smista-storage = { workspace = true }
surrealdb = { workspace = true, features = ["protocol-http", "protocol-ws"] }
testcontainers = { workspace = true }
tokio = { workspace = true, features = ["full"] }
